library(diffrprojects)

dongs <- rtext$new(file=dp_tf(1))
dings <- rtext$new(text="1234567890")


dings$delete(2)$get_text()

# get_token()

# deleting text
# insert text
# replace text

# entering data
# char data to token data
# token data to char data

# token_aliases?

# plot text - polygons?
